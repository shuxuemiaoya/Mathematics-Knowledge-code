interface:
  display_name: "Book Graph Markdown"
  short_description: "Standardize split book Markdown"
  default_prompt: "Use $book-graph-markdown to standardize every split note, convert all functional textbook blocks and worked examples into valid callouts, and preserve protected content and paths."
