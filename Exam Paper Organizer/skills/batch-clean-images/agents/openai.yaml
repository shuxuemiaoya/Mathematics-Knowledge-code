interface:
  display_name: "Batch Clean Images"
  short_description: "Back up and clean images in place without link edits"
  default_prompt: "Use $batch-clean-images to back up every original image, clean each image sequentially, and replace it in place without changing Markdown paths."
