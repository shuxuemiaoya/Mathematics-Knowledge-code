interface:
  display_name: "Render Exam LaTeX PDFs"
  short_description: "Final gated publishing for exam PDFs"
  default_prompt: "Use $render-exam-latex-pdfs as the final publishing stage after organizer prerequisites pass, or explicitly as a standalone renderer."
