interface:
  display_name: "Render Exam LaTeX PDFs"
  short_description: "Publish exam PDFs with selectable LaTeX styles"
  default_prompt: "Use $render-exam-latex-pdfs to publish the selected exam editions with the requested paired template style after prerequisites pass, or explicitly in standalone mode."
