interface:
  display_name: "Book to Wiki Graph"
  short_description: "Build audited atom-concept book maps"
  default_prompt: "Use $book-to-wiki-graph to build independently readable and fully attributed organizer indexes, put section-wide framing questions before sibling knowledge topics, attach inline practice to its target topic, let the LLM exclusively decide knowledge-atom count and boundaries (including parallel formal definitions without transition words), jointly review category-specific atom boundaries and local teaching logic, invoke $knowledge-relation-mapper before materialization, then write fully attributed atoms, definition-only named concept cards, and three-level knowledge constellations."
