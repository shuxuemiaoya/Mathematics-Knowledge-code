interface:
  display_name: "Book to Wiki Graph"
  short_description: "Build audited atom-concept book maps"
  default_prompt: "Use $book-to-wiki-graph to build independently readable and fully attributed organizer indexes; distinguish whole-book, chapter, section, and single-topic introductions; keep each scoped introduction source-complete; reject organizer subtrees that cross printed headings or leave instructional sections with exercises only; let the LLM exclusively decide knowledge boundaries; jointly review category-specific atoms and local teaching logic; invoke $knowledge-relation-mapper before materialization; then write fully attributed atoms, definition-only named concept cards, and three-level knowledge constellations."
