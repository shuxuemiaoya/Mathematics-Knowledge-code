interface:
  display_name: "Question Type Graph"
  short_description: "Build and repair structured question graphs"
  default_prompt: "Use $question-type-graph to convert or repair these books as an audited, correctly nested atomic-question graph."
